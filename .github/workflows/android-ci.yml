name: Android CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.6.0
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.0"

      - name: Unit tests
        run: gradle test --stacktrace

      - name: Verify Room schema is committed
        run: |
          changes="$(git status --porcelain -- storage/database/schemas)"
          if [ -n "$changes" ]; then
            echo "Room schema changed or is untracked. Commit the generated schema before merging."
            echo "$changes"
            exit 1
          fi

      - name: Upload Room schemas
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: room-schemas
          path: storage/database/schemas
          if-no-files-found: warn

      - name: Android lint
        run: gradle lint --stacktrace

      - name: Assemble debug APK
        run: gradle assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: article-navigator-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
